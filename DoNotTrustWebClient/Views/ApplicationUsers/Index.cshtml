@model DoNotTrustWebClient.Models.ApplicationUsersViewModels.IndexViewModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<h3>Filtr</h3>
<form method="post">
	<table class="table">
		<tr>
			<td>
				<select asp-for="DepartmentId" asp-items="Model.Departments"></select>
			</td>
			<td>
				<input type="submit" value="Filtruj" />
			</td>
		</tr>
	</table>
</form>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Users.First().UserName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Users.First().Department.Company)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Users.First().Department)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Users) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Department.Company.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Department.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
